{% assign show_announcement = section.settings.show_announcement %}

<div class="relative overflow-hidden {% if show_announcement %}mt-16{% else %}mt-24{% endif %}">
  <!-- Decorative Elements -->
  <div class="absolute top-20 right-20 w-64 h-64 bg-natural-gradient rounded-full opacity-20 blur-3xl animate-float"></div>
  <div class="absolute bottom-20 left-20 w-48 h-48 bg-accent-gradient rounded-full opacity-20 blur-3xl animate-float" style="animation-delay: 1s"></div>
  
  <div class="max-w-7xl mx-auto">
    <div class="relative z-10 pb-8 sm:pb-16 md:pb-20 lg:w-full lg:pb-28 xl:pb-32">
      <div class="relative pt-6 px-4 sm:px-6 lg:px-8"></div>
      
      <main class="mt-10 mx-auto max-w-7xl px-4 sm:mt-12 sm:px-6 md:mt-16 lg:mt-20 lg:px-8 xl:mt-28">
        <div class="sm:text-center lg:text-left animate-fade-in">
          <div class="inline-flex items-center px-6 py-2 rounded-full bg-emerald-500/20 text-emerald-800 mb-8 backdrop-blur-sm hover:bg-emerald-500/30 transition-colors duration-300">
            <span class="text-lg font-medium">✨ Transform Your Life Naturally</span>
          </div>
          
          <h1 class="text-4xl tracking-tight font-extrabold sm:text-5xl md:text-6xl">
            <span class="block xl:inline text-transparent bg-clip-text bg-natural-gradient">{{ section.settings.heading }}</span>
            <span class="block text-transparent bg-clip-text bg-accent-gradient xl:inline">{{ section.settings.subheading }}</span>
          </h1>
          
          <p class="mt-3 text-base text-gray-600 sm:mt-5 sm:text-lg sm:max-w-xl sm:mx-auto md:mt-5 md:text-xl lg:mx-0 glass-effect p-6 rounded-xl backdrop-blur-md bg-white/5">
            {{ section.settings.description }}
          </p>
          
          <div class="mt-5 sm:mt-8 sm:flex sm:justify-center lg:justify-start space-x-4">
            {% if section.settings.button_label != blank %}
              <div class="transform hover:scale-105 transition-all duration-300">
                <a href="{{ section.settings.button_link }}" 
                   class="w-full flex items-center justify-center px-8 py-3 border border-transparent text-base font-medium rounded-xl text-white bg-natural-gradient hover:opacity-90 md:py-4 md:text-lg md:px-10 shadow-lg hover:shadow-xl transition-all duration-300">
                  {{ section.settings.button_label }}
                </a>
              </div>
            {% endif %}
            
            {% if section.settings.secondary_button_label != blank %}
              <div class="transform hover:scale-105 transition-all duration-300">
                <a href="{{ section.settings.secondary_button_link }}" 
                   class="w-full flex items-center justify-center px-8 py-3 border border-transparent text-base font-medium rounded-xl text-gray-700 glass-effect backdrop-blur-sm hover:bg-white/20 md:py-4 md:text-lg md:px-10 shadow-md hover:shadow-lg transition-all duration-300">
                  {{ section.settings.secondary_button_label }}
                </a>
              </div>
            {% endif %}
          </div>
        </div>
      </main>
    </div>
  </div>
  
  <div class="lg:absolute lg:inset-y-0 lg:right-0 lg:w-1/2">
    {% if section.settings.image != blank %}
      <div class="relative overflow-hidden rounded-l-3xl shadow-2xl">
        <div class="absolute inset-0 bg-gradient-to-r from-emerald-500/20 to-transparent mix-blend-overlay"></div>
        <img class="h-56 w-full object-cover sm:h-72 md:h-96 lg:w-full lg:h-full transform hover:scale-105 transition-transform duration-700" 
             src="{{ section.settings.image | img_url: '1000x' }}" 
             alt="{{ section.settings.image.alt | escape }}">
      </div>
    {% else %}
      <div class="h-56 w-full bg-gradient-to-r from-emerald-500 to-blue-500 sm:h-72 md:h-96 lg:w-full lg:h-full rounded-l-3xl"></div>
    {% endif %}
  </div>
</div>

{% schema %}
{
  "name": "Hero Section",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Transform Your Health"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "Subheading",
      "default": "With Natural Solutions"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Description",
      "default": "Discover our range of natural wellness products designed to help you achieve optimal health and vitality."
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Button label",
      "default": "Shop Now"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button link"
    },
    {
      "type": "text",
      "id": "secondary_button_label",
      "label": "Secondary button label",
      "default": "Learn More"
    },
    {
      "type": "url",
      "id": "secondary_button_link",
      "label": "Secondary button link"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Hero image"
    },
    {
      "type": "checkbox",
      "id": "show_announcement",
      "label": "Has announcement bar",
      "default": true
    }
  ],
  "presets": [
    {
      "name": "Hero",
      "category": "Hero"
    }
  ]
}
{% endschema %}

{% stylesheet %}
.text-gradient {
  background: linear-gradient(135deg, var(--gradient-start), var(--gradient-end));
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

.bg-natural-gradient {
  background: linear-gradient(135deg, var(--natural-gradient-start), var(--natural-gradient-end));
}

.bg-accent-gradient {
  background: linear-gradient(135deg, var(--accent-gradient-start), var(--accent-gradient-end));
}

.glass-effect {
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(10px);
  border: 1px solid rgba(255, 255, 255, 0.2);
}

@keyframes float {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-10px); }
}

.animate-float {
  animation: float 3s ease-in-out infinite;
}

@keyframes fade-in {
  from {
    opacity: 0;
    transform: translateY(-10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.animate-fade-in {
  animation: fade-in 0.5s ease-out forwards;
}
{% endstylesheet %}
