{% comment %}
  Converted from Navbar.tsx to Shopify section
{% endcomment %}

<nav class="bg-white shadow-lg fixed w-full top-0 z-50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16">
      <div class="flex-shrink-0 flex items-center">
        <a href="/" class="text-2xl font-bold text-gradient">
          {{ shop.name }}
        </a>
      </div>
      
      <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
        <a href="/" class="inline-flex items-center px-1 pt-1 text-gray-900">Home</a>
        <a href="/products" class="inline-flex items-center px-1 pt-1 text-gray-900">Products</a>
        <a href="/pages/process" class="inline-flex items-center px-1 pt-1 text-gray-900">Process</a>
        <a href="/pages/benefits" class="inline-flex items-center px-1 pt-1 text-gray-900">Benefits</a>
      </div>

      <div class="flex items-center">
        <a href="/cart" class="p-2 text-gray-900">
          Cart ({{ cart.item_count }})
        </a>
      </div>
    </div>
  </div>
</nav>

{% schema %}
{
  "name": "Header",
  "settings": [
    {
      "type": "text",
      "id": "menu",
      "label": "Menu"
    }
  ]
}
{% endschema %}
